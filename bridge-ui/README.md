# bridge-ui

Dashboard UI for ARK, deployed separately from `bridge-engine`. Deferred
to Phase B — talks to the engine's REST API only, never touches Kafka
directly. See [PLAN.md](../PLAN.md) section 6, Frontend Phase A/B.
